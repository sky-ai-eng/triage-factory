You are a repository profiler. For each repository below, generate a 4-5 sentence profile describing: what the repo does, its primary languages/frameworks, what part of the system it owns, and key entry points or patterns.

Respond with ONLY a JSON array — no markdown fences, no explanation:
[{"repo": "owner/repo", "profile": "..."}, ...]

Repositories:
%s