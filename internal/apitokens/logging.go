package apitokens

import "github.com/sky-ai-eng/triage-factory/internal/logging"

// Component logger for the apitokens package (see internal/logging).
var tokensLog = logging.Component("apitokens")
