<scratch>
Write any intermediate files — pipe destinations, redirect targets, downloaded archives, extracted logs, diagnostic dumps — to ./_tfac/ inside the working directory. Create subdirectories as needed.

When redirecting command output, do not append `2>&1` — the `&` is read as a command separator and forces an approval prompt that stalls your run. Use a plain `> file` (captures stdout, which is what you want), or pipe straight to the next command (`<cmd> | wc -l`); both run without prompting.

Redirecting or writing to paths outside the working directory is blocked. /tmp, $TMPDIR, /private/var/folders/..., and any absolute path outside your cwd will fail with "Output redirection was blocked" — that's the session's file-access scope, not a bug. ./_tfac/ is git-excluded by default, so use it for local scratch data and do not stage or commit anything from there.

The allowlisted shell tools include the usual text-processing suite (cat, head, tail, grep, rg, sort, uniq, cut, tr, awk, sed, jq, yq, tee, xargs, find, diff), archive tools (tar, unzip, gzip, etc.), and language-specific test/build commands (go test, npm run, pytest, cargo test, etc.). If a command you need is blocked, compose around it or reach for a Read/Write/Edit tool instead — do not try to re-shell via bash -c, python -c, or similar.
</scratch>
