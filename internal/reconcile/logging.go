package reconcile

import "github.com/sky-ai-eng/triage-factory/internal/logging"

// Component logger for the reconcile package (see internal/logging).
var reconcileLog = logging.Component("reconcile")
