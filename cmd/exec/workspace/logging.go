package workspace

import "github.com/sky-ai-eng/triage-factory/internal/logging"

// Component logger for the workspace exec subcommand (see internal/logging).
var workspaceLog = logging.Component("workspace")
