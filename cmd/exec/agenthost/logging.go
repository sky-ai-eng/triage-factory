package agenthost

import "github.com/sky-ai-eng/triage-factory/internal/logging"

// Component logger for the agenthost package (see internal/logging).
var agenthostLog = logging.Component("agenthost")
